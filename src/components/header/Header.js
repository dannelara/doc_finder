import React from "react";
import { P } from "../../utils";
import { StyledHeader } from "./Styles";

export default function Header() {
  return (
    <StyledHeader>
      <P type="header">Document finder</P>
    </StyledHeader>
  );
}
